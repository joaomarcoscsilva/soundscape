defaults:
  - /experiment: resnet_leec12.yaml

seed: 1
num_runs: 100
keep_model: true

sgd_chance: 0.9

log_lr:
  min: -2
  max: -1

log_wd:
  min: -3
  max: 0

sub_log_momentum:
  min: -3
  max: 0

augment:
  chance: 1.0

  mixup:
    min: 0.5
    max: 2.0

  cutmix:
    min: 0.0
    max: 0.5

  random_crop_chance: 0.1

  cropped_length:
    min: 3
    max: 3
